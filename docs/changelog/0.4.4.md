# [0.4.4] — Unreleased

Follow-up fixes discovered while re-verifying the 0.4.3 fixes end-to-end against a real cluster
(`docs/test-plan:1.1/`, `docs/report:1.1/`). Two of the 0.4.3 fixes (ISS-025 in particular) had scope
gaps that surfaced as new regressions once re-tested at the HTTP-route/capstone level rather than just
direct-repository unit tests; one further pre-existing bug (ISS-030) became reachable/observable for
the first time once ISS-029 was fixed.

### Fixed

- [ISS-030](../issues/ISS-030-soft-delete-removes-lookup-rows.md) — `BatchEngine`'s soft-delete
  unconditionally deleted `@LookupIndex` rows, contradicting the documented "soft delete does not
  remove lookup rows" behavior — a soft-deleted row still "exists" until its TTL expires, so it should
  remain resolvable via its lookup index too.
